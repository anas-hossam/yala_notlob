<h1>Editing Item</h1>

<%= render 'form', item: @item %>

<%= link_to 'Show', order_item_path(@order), :class => 'btn btn-sm btn-success' %> |
<%= link_to 'Back', order_items_path(@order), :class => 'btn btn-sm btn-success' %>
